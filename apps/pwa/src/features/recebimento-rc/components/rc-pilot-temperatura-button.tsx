import { TemperaturaRcModalButton } from './temperatura-rc-modal-button';

export function RcTemperaturaButton({ demandId }: { demandId: string }) {
  return <TemperaturaRcModalButton demandId={demandId} />;
}
