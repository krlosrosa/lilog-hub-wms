/**
 * Accent badges on tinted backgrounds must use `text-accent`.
 * `text-accent-foreground` is only for solid `bg-accent` surfaces.
 */
export const accentSubtleBadgeClassName =
  'bg-accent/15 text-accent ring-accent/20';

export const accentSubtleBadgePlainClassName = 'bg-accent/15 text-accent';

export const accentSubtleBadgeBorderClassName =
  'border-accent/35 bg-accent/10 text-accent';

export const accentSubtleNavActiveClassName =
  'bg-accent/15 font-semibold text-accent';

export const accentSubtleIconClassName = 'bg-accent/10 text-accent';
