# Sentry — projeto lilog-web (Settings > Projects > lilog-web > Client Keys)
NEXT_PUBLIC_SENTRY_DSN=

# Auth token para upload de source maps no build (Settings > Auth Tokens)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=lilog-web
