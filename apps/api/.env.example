DATABASE_URL=postgres://user:pass@localhost:5432/lilog

JWT_SECRET=supersecret

# Cookie de sessão — necessário quando web (admin-dev) e API (api-dev) estão em subdomínios diferentes
# COOKIE_DOMAIN=.logistica-processo.com
# COOKIE_SECURE=true

PORT=3001

CORS_ORIGIN=http://localhost:3000,http://localhost:5174,http://localhost:5175,http://192.168.1.3:5174,http://192.168.1.3:5175

# URL pública do PWA (link de rastreio para motorista)
PWA_BASE_URL=http://localhost:5174

NODE_ENV=development

# Sentry — projeto lilog-api (Settings > Projects > lilog-api > Client Keys)
SENTRY_DSN=

REDIS_HOST=localhost

REDIS_PORT=6379

# REDIS_PASSWORD=your-redis-password

RAVEX_BASE_URL=https://api.rest.app.ravex.com.br
RAVEX_USERNAME=seu-email@ravex.com.br
RAVEX_PASSWORD=sua-senha

# Armazenamento de objetos via API S3-compatível (@aws-sdk/client-s3)
# Suporta MinIO (dev) ou Cloudflare R2 (prod).
#
# --- MinIO (recomendado em dev) ---
R2_S3_ENDPOINT=http://localhost:9000
R2_ACCESS_KEY_ID=minioadmin
R2_SECRET_ACCESS_KEY=minioadmin
R2_BUCKET_NAME=lilog-wms
R2_REGION=us-east-1
R2_PUBLIC_URL=http://localhost:9000/lilog-wms
#
# --- Cloudflare R2 (alternativa prod) ---
# R2_ACCOUNT_ID=
# R2_API_TOKEN=          # token cfat_... exibido na criação
# R2_BUCKET_NAME=
# R2_PUBLIC_URL=
# Ou credenciais S3 explícitas do R2:
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
# R2_TOKEN_ID=
# R2_REGION=auto
# URL pública da API (uploads locais, links absolutos)
API_PUBLIC_URL=http://localhost:3001
# Swagger e Bull Board (filas) — derivados da API em dev/staging
SWAGGER_PUBLIC_URL=http://localhost:3001/api/docs
BULL_BOARD_PUBLIC_URL=http://localhost:3001/queues

# Resend (envio de email — requer RESEND_API_KEY)
RESEND_API_KEY=
# Opcional: remetente customizado (domínio verificado no Resend)
# EMAIL_FROM=noreply@logistica-processo.com
